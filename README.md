# umi-tools

The tools used to build umi.

## Usage

```bash
$ umi-tools build
$ umi-tools build --watch  # -w
$ umi-tools build --sourcemaps  # -s
$ umi-tools test
$ umi-tools rollup  # -w -s
```

## LICENSE

MIT
